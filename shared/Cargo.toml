[package]
name = "shared"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dllinject = { git = "https://github.com/amrbashir/dllinject" }
windows = { workspace = true }
